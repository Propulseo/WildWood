---
phase: 06-secondary-modules
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Newsletter page shows a contact list with name, email, date added, and source"
    - "User can create a campaign draft with title, subject line, and rich text body"
    - "Newsletter page shows a mock history of past campaigns with date, subject, and number of recipients"
    - "Instagram page displays follower count with 30-day evolution and engagement rate cards"
    - "Instagram page shows a top-5 posts table by engagement with thumbnail and stats and a Recharts line chart of follower growth over 3/6/12 month toggles"
  artifacts:
    - path: "src/app/(admin)/newsletter/page.tsx"
      provides: "Newsletter page"
    - path: "src/app/(admin)/instagram/page.tsx"
      provides: "Instagram page"
  key_links:
    - from: "src/app/(admin)/newsletter/page.tsx"
      to: "src/lib/data-access.ts"
      via: "getNewsletterContacts, getNewsletterCampaigns"
      pattern: "getNewsletter"
    - from: "src/app/(admin)/instagram/page.tsx"
      to: "src/lib/data-access.ts"
      via: "getInstagramStats"
      pattern: "getInstagramStats"
---

<objective>
Verify Phase 6 is complete: automated checks + visual inspection of Newsletter and Instagram pages.

Purpose: Confirm all 7 requirements (NEWS-01, NEWS-02, NEWS-03, INSTA-01, INSTA-02, INSTA-03, INSTA-04) are satisfied before marking Phase 6 complete.
Output: Verification results and phase completion confirmation.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-secondary-modules/06-02-SUMMARY.md
@.planning/phases/06-secondary-modules/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of Phase 6</name>
  <files></files>
  <action>
    Run the following verification checks:

    1. **Type check:** `npx tsc --noEmit` -- must pass with zero errors

    2. **Build:** `npm run build` -- must succeed, verify /newsletter and /instagram routes appear in build output

    3. **Types verification:** Confirm types.ts exports ContactNewsletter, CampagneNewsletter, InstagramPost, InstagramStats

    4. **Data layer verification:**
       - newsletter-contacts.json exists with 18 entries
       - newsletter-campaigns.json exists with 6 entries
       - instagram-stats.json exists with topPosts array (5 items) and historiqueAbonnes array (12+ items)
       - data-access.ts exports getNewsletterContacts, getNewsletterCampaigns, getInstagramStats

    5. **Component verification:**
       - src/components/newsletter/contacts-table.tsx exists and imports Table
       - src/components/newsletter/campagne-dialog.tsx exists and imports Dialog
       - src/components/newsletter/campagnes-historique.tsx exists and imports Table
       - src/components/instagram/stats-cards.tsx exists and imports Card
       - src/components/instagram/top-posts-table.tsx exists and imports Table
       - src/components/instagram/follower-chart.tsx exists and imports recharts

    6. **Requirement coverage check:**
       - NEWS-01: contacts-table.tsx renders nom, email, dateAjout, source columns
       - NEWS-02: campagne-dialog.tsx has titre, objet, and textarea inputs
       - NEWS-03: campagnes-historique.tsx renders dateEnvoi, objet, nbDestinataires columns
       - INSTA-01: stats-cards.tsx renders abonnes and evolutionAbonnes30j
       - INSTA-02: stats-cards.tsx renders tauxEngagement and porteeMoyenne
       - INSTA-03: top-posts-table.tsx renders 5 posts with couleurVignette and stats
       - INSTA-04: follower-chart.tsx imports LineChart from recharts and has 3/6/12 toggle

    Report results as a checklist.
  </action>
  <verify>All 7 requirement checks pass. Build succeeds.</verify>
  <done>Automated verification confirms all NEWS-* and INSTA-* requirements are implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Newsletter and Instagram admin pages -- the two secondary modules for Phase 6:

    1. **Newsletter (/newsletter):**
       - Contacts tab: table with 18 contacts (nom, email, date, source badge)
       - Campagnes tab: table with 6 campaign history entries (date, subject, recipients, status)
       - "Nouvelle campagne" dialog with titre, objet, and textarea body

    2. **Instagram (/instagram):**
       - 4 stat cards: abonnes (2,847), evolution 30j (+127), taux engagement (4.2%), portee moyenne
       - Top 5 posts table with colored thumbnail squares and engagement stats
       - Follower growth line chart with 3/6/12 month period toggles
  </what-built>
  <how-to-verify>
    Start dev server if not running: `npm run dev`

    **Newsletter page:**
    1. Navigate to http://localhost:3000/newsletter (login as admin if needed)
    2. Verify "Contacts" tab is active by default, showing a table of contacts with name, email, date, and source badges
    3. Click "Campagnes" tab -- verify campaign history table appears with dates, subjects, recipient counts, and status badges
    4. Click "Nouvelle campagne" button -- verify dialog opens with titre, objet, and message body textarea
    5. Fill in all fields and submit -- verify toast "Brouillon enregistre" appears

    **Instagram page:**
    6. Navigate to http://localhost:3000/instagram
    7. Verify 4 stat cards display at the top (abonnes, evolution 30j, engagement, portee)
    8. Verify top-5 posts table shows colored squares for thumbnails and engagement stats
    9. Verify follower growth chart is visible with a line graph
    10. Click "3 mois" toggle -- chart should show only the last 3 months of data
    11. Click "6 mois" -- chart shows 6 months
    12. Click "12 mois" -- chart shows full year

    **Sidebar:**
    13. Verify "Newsletter" and "Instagram" links in the admin sidebar are functional and show active state
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 6, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 6 success criteria from roadmap:
1. Newsletter page shows contact list with name, email, date added, source
2. User can create campaign draft with title, subject line, rich text body
3. Newsletter page shows mock history of past campaigns with date, subject, recipients
4. Instagram page displays follower count with 30-day evolution and engagement rate cards
5. Instagram page shows top-5 posts table and Recharts follower growth chart with 3/6/12 toggles
</verification>

<success_criteria>
- All automated checks pass (types, build, requirements)
- Human verifies both pages render correctly with all expected data
- Newsletter dialog works end-to-end (open, fill, submit, toast)
- Instagram chart toggles function correctly
- Phase 6 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-secondary-modules/06-04-SUMMARY.md`
</output>
