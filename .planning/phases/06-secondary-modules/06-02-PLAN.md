---
phase: 06-secondary-modules
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(admin)/newsletter/page.tsx
  - src/components/newsletter/contacts-table.tsx
  - src/components/newsletter/campagne-dialog.tsx
  - src/components/newsletter/campagnes-historique.tsx
autonomous: true

must_haves:
  truths:
    - "Newsletter page shows a contact list with name, email, date added, and source"
    - "User can create a campaign draft with title, subject line, and rich text body"
    - "Newsletter page shows mock history of past campaigns with date, subject, and recipients count"
  artifacts:
    - path: "src/app/(admin)/newsletter/page.tsx"
      provides: "Newsletter page with contacts table, campaign dialog trigger, campaign history"
      min_lines: 20
    - path: "src/components/newsletter/contacts-table.tsx"
      provides: "Table listing newsletter contacts with nom, email, dateAjout, source"
      min_lines: 30
    - path: "src/components/newsletter/campagne-dialog.tsx"
      provides: "Dialog form for creating a campaign draft with titre, objet, corps (textarea)"
      min_lines: 40
    - path: "src/components/newsletter/campagnes-historique.tsx"
      provides: "Table of past campaigns with date, subject, nb recipients"
      min_lines: 25
  key_links:
    - from: "src/components/newsletter/contacts-table.tsx"
      to: "src/lib/data-access.ts"
      via: "getNewsletterContacts called in parent page"
      pattern: "getNewsletterContacts"
    - from: "src/components/newsletter/campagnes-historique.tsx"
      to: "src/lib/data-access.ts"
      via: "getNewsletterCampaigns called in parent page"
      pattern: "getNewsletterCampaigns"
    - from: "src/components/newsletter/campagne-dialog.tsx"
      to: "src/components/ui/dialog.tsx"
      via: "shadcn Dialog component"
      pattern: "import.*Dialog.*from.*ui/dialog"
---

<objective>
Build the Newsletter admin page with a contacts table, campaign creation dialog, and campaign history.

Purpose: Satisfies NEWS-01 (contact list), NEWS-02 (campaign form), NEWS-03 (campaign history). The newsletter module gives the owner visibility into their contact base and the ability to draft campaigns (no actual sending -- prototype).
Output: Newsletter page at /newsletter with 3 subcomponents.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-secondary-modules/06-01-SUMMARY.md
@src/lib/types.ts
@src/lib/data-access.ts
@src/app/(admin)/comptabilite/page.tsx
@src/components/comptabilite/depense-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contacts table and campaign history components</name>
  <files>
    src/components/newsletter/contacts-table.tsx
    src/components/newsletter/campagnes-historique.tsx
  </files>
  <action>
    **Create src/components/newsletter/contacts-table.tsx:**
    - 'use client' component
    - Props: `{ contacts: ContactNewsletter[] }`
    - Import `ContactNewsletter` from `@/lib/types`
    - Use shadcn Table (Table, TableHeader, TableRow, TableHead, TableBody, TableCell) from `@/components/ui/table`
    - Columns: Nom (prenom + nom combined), Email, Date d'ajout (formatted as DD/MM/YYYY using date-fns `format(parseISO(date), 'dd/MM/yyyy')`), Source (display as Badge with variant: profil-client = default, inscription-web = secondary, ajout-manuel = outline)
    - Import Badge from `@/components/ui/badge`
    - Show contact count as subtitle text above table: "{N} contacts inscrits"
    - Sort by dateAjout descending (most recent first)

    **Create src/components/newsletter/campagnes-historique.tsx:**
    - 'use client' component
    - Props: `{ campagnes: CampagneNewsletter[] }`
    - Import `CampagneNewsletter` from `@/lib/types`
    - Use shadcn Table
    - Columns: Date (formatted DD/MM/YYYY), Objet, Destinataires, Statut (Badge: envoyee = default, brouillon = secondary)
    - Sort by dateEnvoi descending
    - Section title: "Historique des campagnes"
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors.
    Both components import from @/lib/types and @/components/ui/table.
  </verify>
  <done>
    ContactsTable renders a sorted table of newsletter contacts with formatted dates and source badges. CampagnesHistorique renders a sorted table of past campaigns with status badges.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create campaign dialog and newsletter page</name>
  <files>
    src/components/newsletter/campagne-dialog.tsx
    src/app/(admin)/newsletter/page.tsx
  </files>
  <action>
    **Create src/components/newsletter/campagne-dialog.tsx:**
    - 'use client' component
    - Follow the EXACT pattern from depense-dialog.tsx (shadcn Dialog with trigger Button)
    - Dialog trigger: Button with "Nouvelle campagne" text (not a + icon)
    - Form fields inside DialogContent:
      1. Titre (Input, required)
      2. Objet (Input -- subject line, required)
      3. Corps du message (textarea element with className matching shadcn Input styling: `flex w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50`). Set rows={8} and placeholder="Redigez votre message...".
      4. Do NOT use a WYSIWYG editor -- a styled textarea is sufficient for the prototype
    - On submit: show a sonner toast "Brouillon enregistre" and close dialog. No actual persistence needed (prototype). Use `toast` from `sonner`.
    - DialogTitle: "Nouvelle campagne"
    - DialogDescription: "Creez un brouillon de campagne newsletter"
    - Use controlled open state (useState for open) to close on submit
    - Import Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter from @/components/ui/dialog
    - Import Button from @/components/ui/button, Input from @/components/ui/input, Label from @/components/ui/label
    - Import toast from sonner

    **Create src/app/(admin)/newsletter/page.tsx:**
    - 'use client' page (needs useState/useEffect for data loading)
    - Load data with useEffect on mount:
      ```
      const [contacts, setContacts] = useState<ContactNewsletter[]>([])
      const [campagnes, setCampagnes] = useState<CampagneNewsletter[]>([])
      useEffect(() => {
        getNewsletterContacts().then(setContacts)
        getNewsletterCampaigns().then(setCampagnes)
      }, [])
      ```
    - Page layout following comptabilite page pattern:
      - Header: h1 "Newsletter" (font-display text-3xl font-bold), subtitle "Gestion des contacts et campagnes" (text-muted-foreground)
      - CampagneDialog button in the top-right (flex items-start justify-between, same as comptabilite)
    - Use shadcn Tabs with two tabs: "Contacts" (default) and "Campagnes"
      - Contacts tab: render ContactsTable with contacts prop
      - Campagnes tab: render CampagnesHistorique with campagnes prop
    - Import Tabs, TabsList, TabsTrigger, TabsContent from @/components/ui/tabs
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors.
    Run `npm run build` -- successful build, /newsletter route appears in output.
    Verify the page imports and renders all 3 subcomponents.
  </verify>
  <done>
    Newsletter page at /newsletter shows contacts table (default tab) and campaigns history tab, with a "Nouvelle campagne" dialog button. Campaign form has titre, objet, and corps textarea with toast confirmation.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds with /newsletter route
3. Newsletter page uses Tabs pattern matching comptabilite page
4. Campaign dialog follows depense-dialog pattern with controlled open state
5. Contacts table shows name, email, date, source with Badge components
6. Campaign history table shows date, subject, recipients, status
</verification>

<success_criteria>
- /newsletter route renders with two tabs (Contacts, Campagnes)
- Contacts table displays 18 contacts with formatted dates and source badges
- Campaign dialog opens, accepts titre/objet/corps, shows toast on submit
- Campaign history shows 6 campaigns with status badges
- Zero type errors, successful build
</success_criteria>

<output>
After completion, create `.planning/phases/06-secondary-modules/06-02-SUMMARY.md`
</output>
