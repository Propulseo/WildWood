---
phase: 06-secondary-modules
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(admin)/instagram/page.tsx
  - src/components/instagram/stats-cards.tsx
  - src/components/instagram/top-posts-table.tsx
  - src/components/instagram/follower-chart.tsx
autonomous: true

must_haves:
  truths:
    - "Instagram page displays follower count with 30-day evolution and engagement rate cards"
    - "Instagram page shows a top-5 posts table by engagement with thumbnail and stats"
    - "Instagram page shows a Recharts line chart of follower growth with 3/6/12 month toggles"
  artifacts:
    - path: "src/app/(admin)/instagram/page.tsx"
      provides: "Instagram analytics page with stat cards, top posts, follower chart"
      min_lines: 25
    - path: "src/components/instagram/stats-cards.tsx"
      provides: "Follower count card, 30-day evolution card, engagement rate card, average reach card"
      min_lines: 25
    - path: "src/components/instagram/top-posts-table.tsx"
      provides: "Table of top 5 posts with colored thumbnail div, caption, likes, comments, reach"
      min_lines: 30
    - path: "src/components/instagram/follower-chart.tsx"
      provides: "Recharts LineChart with follower growth and 3/6/12 month toggle"
      min_lines: 40
  key_links:
    - from: "src/app/(admin)/instagram/page.tsx"
      to: "src/lib/data-access.ts"
      via: "getInstagramStats in useEffect"
      pattern: "getInstagramStats"
    - from: "src/components/instagram/follower-chart.tsx"
      to: "recharts"
      via: "LineChart import"
      pattern: "import.*LineChart.*from.*recharts"
    - from: "src/components/instagram/top-posts-table.tsx"
      to: "src/lib/types.ts"
      via: "InstagramPost type"
      pattern: "import.*InstagramPost.*from.*types"
---

<objective>
Build the Instagram analytics page with stat cards, top posts table, and follower growth chart.

Purpose: Satisfies INSTA-01 (follower count + 30-day evolution), INSTA-02 (engagement rate + reach), INSTA-03 (top 5 posts), INSTA-04 (follower growth chart with period toggles). Gives the owner a quick view of their Instagram performance.
Output: Instagram page at /instagram with 3 subcomponents.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-secondary-modules/06-01-SUMMARY.md
@src/lib/types.ts
@src/lib/data-access.ts
@src/components/comptabilite/vue-mensuelle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stat cards and top posts table components</name>
  <files>
    src/components/instagram/stats-cards.tsx
    src/components/instagram/top-posts-table.tsx
  </files>
  <action>
    **Create src/components/instagram/stats-cards.tsx:**
    - 'use client' component
    - Props: `{ abonnes: number; evolutionAbonnes30j: number; tauxEngagement: number; porteeMoyenne: number }`
    - Render 4 shadcn Card components in a responsive grid: `grid grid-cols-2 lg:grid-cols-4 gap-4`
    - Card 1 - "Abonnes": Display `abonnes` formatted with toLocaleString('fr-FR'). Large text.
    - Card 2 - "Evolution 30j": Display `+{evolutionAbonnes30j}` with green text (text-green-600). Show as positive change indicator.
    - Card 3 - "Taux d'engagement": Display `{tauxEngagement}%`.
    - Card 4 - "Portee moyenne": Display `porteeMoyenne` formatted with toLocaleString('fr-FR').
    - Each card: CardHeader with CardTitle (text-sm font-medium text-muted-foreground), CardContent with the value in text-2xl font-bold.
    - Import Card, CardHeader, CardTitle, CardContent from @/components/ui/card

    **Create src/components/instagram/top-posts-table.tsx:**
    - 'use client' component
    - Props: `{ posts: InstagramPost[] }`
    - Import `InstagramPost` from `@/lib/types`
    - Use shadcn Table
    - Section title: "Top 5 posts par engagement" (h2, font-display text-xl font-semibold, mb-4)
    - Columns: Vignette, Legende, Likes, Commentaires, Portee
    - Vignette column: Render a colored div with `width: 48px, height: 48px, borderRadius: 6px, backgroundColor: post.couleurVignette`. Use inline style for the dynamic color.
    - Legende column: Truncate to ~50 chars with CSS (`max-w-[200px] truncate`)
    - Likes, Commentaires, Portee: Right-aligned numbers formatted with toLocaleString('fr-FR')
    - Sort posts by (likes + commentaires) descending before rendering (engagement = likes + commentaires)
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors.
    Both components import correct types and shadcn components.
  </verify>
  <done>
    StatsCards renders 4 metric cards in a responsive grid. TopPostsTable renders a sorted table of posts with colored placeholder thumbnails and formatted stats.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create follower chart and Instagram page</name>
  <files>
    src/components/instagram/follower-chart.tsx
    src/app/(admin)/instagram/page.tsx
  </files>
  <action>
    **Create src/components/instagram/follower-chart.tsx:**
    - 'use client' component
    - Props: `{ data: { mois: string; abonnes: number }[] }`
    - Import from recharts: LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer
    - Period toggle using 3 buttons (not shadcn Tabs -- use plain buttons with active state styling):
      - "3 mois", "6 mois", "12 mois"
      - State: `useState<3 | 6 | 12>(12)` default to 12 months
      - Active button: `bg-primary text-primary-foreground`, inactive: `bg-muted text-muted-foreground`
      - Button group with `flex gap-1` wrapper, each button with `px-3 py-1 rounded-md text-sm font-medium transition-colors`
    - Filter data to last N months based on selected period (slice from end)
    - Recharts LineChart config:
      - ResponsiveContainer width="100%" height={300}
      - CartesianGrid strokeDasharray="3 3"
      - XAxis dataKey="mois" -- format the month label: parse "2025-04" and display as "Avr 25" using date-fns `format(new Date(mois + '-01'), 'MMM yy', { locale: fr })`. Import `fr` from `date-fns/locale/fr`.
      - YAxis with domain ['dataMin - 50', 'dataMax + 50'] for breathing room
      - Tooltip formatter: `(value: number | string | undefined) => [Number(value).toLocaleString('fr-FR'), 'Abonnes']` (follow the Phase 5 Recharts Tooltip pattern for v3 type compat)
      - Line type="monotone" dataKey="abonnes" stroke="var(--color-primary)" strokeWidth={2} dot={{ r: 3 }}
    - Section title: "Evolution des abonnes" (h2, font-display text-xl font-semibold)
    - Toggle buttons between title and chart

    **Create src/app/(admin)/instagram/page.tsx:**
    - 'use client' page
    - Load data with useEffect:
      ```
      const [stats, setStats] = useState<InstagramStats | null>(null)
      useEffect(() => {
        getInstagramStats().then(setStats)
      }, [])
      if (!stats) return null
      ```
    - Page layout following established pattern:
      - Header: h1 "Instagram" (font-display text-3xl font-bold), subtitle "Statistiques et performance" (text-muted-foreground)
    - Render in order with space-y-6:
      1. StatsCards with abonnes, evolutionAbonnes30j, tauxEngagement, porteeMoyenne props
      2. TopPostsTable with stats.topPosts
      3. FollowerChart with stats.historiqueAbonnes
    - Import InstagramStats from @/lib/types, getInstagramStats from @/lib/data-access
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors.
    Run `npm run build` -- successful build, /instagram route appears in output.
    Verify the page renders all 3 subcomponents with data from getInstagramStats.
  </verify>
  <done>
    Instagram page at /instagram displays 4 stat cards, top-5 posts table with colored thumbnails, and a Recharts line chart with 3/6/12 month toggle. Zero type errors, successful build.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds with /instagram route
3. Stat cards show follower count, 30-day evolution, engagement rate, average reach
4. Top posts table has 5 rows with colored thumbnail divs and right-aligned stats
5. Follower chart uses Recharts LineChart with functioning 3/6/12 month toggle
6. Chart uses date-fns fr locale for French month abbreviations
7. Tooltip follows Phase 5 v3 type compatibility pattern
</verification>

<success_criteria>
- /instagram route renders with stat cards, posts table, and follower chart
- 4 stat cards in responsive 2x2 / 4x1 grid with formatted numbers
- Top-5 posts sorted by engagement with colored placeholder thumbnails
- Recharts LineChart shows follower growth, period toggle filters data correctly
- French month labels on X axis, French number formatting
- Zero type errors, successful build
</success_criteria>

<output>
After completion, create `.planning/phases/06-secondary-modules/06-03-SUMMARY.md`
</output>
