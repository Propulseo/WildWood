---
phase: 06-secondary-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/mock-data/newsletter-contacts.json
  - src/lib/mock-data/newsletter-campaigns.json
  - src/lib/mock-data/instagram-stats.json
  - src/lib/data-access.ts
autonomous: true

must_haves:
  truths:
    - "Newsletter and Instagram types are exported from types.ts"
    - "Mock data files contain realistic French-language newsletter and Instagram data"
    - "data-access.ts exposes getter functions for all new data"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "ContactNewsletter, CampagneNewsletter, InstagramStats, InstagramPost types"
      contains: "interface ContactNewsletter"
    - path: "src/lib/mock-data/newsletter-contacts.json"
      provides: "15+ newsletter contacts with name, email, date, source"
    - path: "src/lib/mock-data/newsletter-campaigns.json"
      provides: "5+ past campaign records with date, subject, recipients count"
    - path: "src/lib/mock-data/instagram-stats.json"
      provides: "Follower stats, engagement data, top posts, monthly follower history"
    - path: "src/lib/data-access.ts"
      provides: "getNewsletterContacts, getNewsletterCampaigns, getInstagramStats functions"
      exports: ["getNewsletterContacts", "getNewsletterCampaigns", "getInstagramStats"]
  key_links:
    - from: "src/lib/data-access.ts"
      to: "src/lib/mock-data/newsletter-contacts.json"
      via: "JSON import"
      pattern: "import.*newsletter-contacts"
    - from: "src/lib/data-access.ts"
      to: "src/lib/mock-data/instagram-stats.json"
      via: "JSON import"
      pattern: "import.*instagram-stats"
---

<objective>
Add TypeScript types, mock data files, and data-access functions for both the Newsletter and Instagram modules.

Purpose: Establish the data foundation that Plan 02 (Newsletter UI) and Plan 03 (Instagram UI) will consume. Follows the established pattern where data-access.ts is the sole importer of mock-data/.
Output: 4 new types in types.ts, 3 new JSON mock data files, 5 new data-access functions.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/types.ts
@src/lib/data-access.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Newsletter and Instagram types to types.ts and create mock data files</name>
  <files>
    src/lib/types.ts
    src/lib/mock-data/newsletter-contacts.json
    src/lib/mock-data/newsletter-campaigns.json
    src/lib/mock-data/instagram-stats.json
  </files>
  <action>
    **Add to src/lib/types.ts** (append after the Role type, keep all existing types untouched):

    1. `ContactNewsletter` interface:
       - `id: string` (use "news-XXX" convention)
       - `prenom: string`
       - `nom: string`
       - `email: string`
       - `dateAjout: string` (ISO YYYY-MM-DD)
       - `source: 'inscription-web' | 'ajout-manuel' | 'profil-client'`

    2. `CampagneNewsletter` interface:
       - `id: string` (use "camp-XXX" convention)
       - `titre: string`
       - `objet: string` (subject line)
       - `dateEnvoi: string` (ISO YYYY-MM-DD)
       - `nbDestinataires: number`
       - `statut: 'envoyee' | 'brouillon'`

    3. `InstagramPost` interface:
       - `id: string` (use "ig-post-XXX" convention)
       - `legende: string` (caption)
       - `datePublication: string` (ISO YYYY-MM-DD)
       - `likes: number`
       - `commentaires: number`
       - `portee: number` (reach)
       - `couleurVignette: string` (hex color for placeholder thumbnail div)

    4. `InstagramStats` interface:
       - `abonnes: number` (follower count)
       - `evolutionAbonnes30j: number` (30-day change, e.g., +127)
       - `tauxEngagement: number` (e.g., 4.2 for 4.2%)
       - `porteeMoyenne: number` (average reach)
       - `topPosts: InstagramPost[]`
       - `historiqueAbonnes: { mois: string; abonnes: number }[]` (monthly data for Recharts, 12 months minimum)

    **Create src/lib/mock-data/newsletter-contacts.json:**
    Array of 18 ContactNewsletter objects. Derive from existing clients who have `newsletter: true` (there are ~20 of them in clients.json). Use matching prenom/nom/email from those clients. Mix sources: ~10 from 'profil-client', ~5 from 'inscription-web', ~3 from 'ajout-manuel'. dateAjout should be within the last 6 months. Keep entries on single lines for compactness.

    **Create src/lib/mock-data/newsletter-campaigns.json:**
    Array of 6 CampagneNewsletter objects. 5 with statut 'envoyee' (past campaigns), 1 with statut 'brouillon'. Realistic French subjects like "Nouveaux cours de yoga en mars", "Offre speciale residents - Mars 2026", "Menu du mois au WildWood Cafe". nbDestinataires between 12 and 18. Dates spread over last 4 months.

    **Create src/lib/mock-data/instagram-stats.json:**
    Single object matching InstagramStats interface:
    - abonnes: 2847
    - evolutionAbonnes30j: 127
    - tauxEngagement: 4.2
    - porteeMoyenne: 1250
    - topPosts: 5 InstagramPost objects with realistic WildWood captions in French ("Sunset depuis le bungalow 3", "Nouveau smoothie bowl tropical", "Session crossfit matinale"), likes 85-340, commentaires 8-42, portee 800-3200, couleurVignette using WildWood-adjacent colors (#C94E0A, #7AB648, #8B6B3D, #2563EB, #D946EF)
    - historiqueAbonnes: 12 monthly entries from April 2025 through March 2026, showing organic growth from ~1800 to 2847. Use format { "mois": "2025-04", "abonnes": 1823 }
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors.
    Verify newsletter-contacts.json has 18 entries, newsletter-campaigns.json has 6 entries, instagram-stats.json has 5 topPosts and 12+ historiqueAbonnes months.
  </verify>
  <done>
    4 new types exported from types.ts. 3 JSON files with realistic French mock data following WildWood conventions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add data-access functions for newsletter and Instagram data</name>
  <files>
    src/lib/data-access.ts
  </files>
  <action>
    **Update src/lib/data-access.ts:**

    1. Add imports at the top (follow existing pattern):
       ```
       import type { ..., ContactNewsletter, CampagneNewsletter, InstagramStats } from './types'
       import newsletterContactsData from './mock-data/newsletter-contacts.json'
       import newsletterCampaignsData from './mock-data/newsletter-campaigns.json'
       import instagramStatsData from './mock-data/instagram-stats.json'
       ```

    2. Add new sections following the established pattern (comment headers like `// --- Newsletter ---`):

       ```typescript
       // --- Newsletter ---

       export async function getNewsletterContacts(): Promise<ContactNewsletter[]> {
         return newsletterContactsData as ContactNewsletter[]
       }

       export async function getNewsletterCampaigns(): Promise<CampagneNewsletter[]> {
         return newsletterCampaignsData as CampagneNewsletter[]
       }

       // --- Instagram ---

       export async function getInstagramStats(): Promise<InstagramStats> {
         return instagramStatsData as InstagramStats
       }
       ```

    Keep all existing functions untouched. Follow the exact same async function pattern used by getClients, getExpenses, etc.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors.
    Run `npm run build` -- successful build with no errors.
  </verify>
  <done>
    data-access.ts exports getNewsletterContacts, getNewsletterCampaigns, and getInstagramStats. Build passes cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. types.ts exports ContactNewsletter, CampagneNewsletter, InstagramPost, InstagramStats
4. data-access.ts has 3 new getter functions importing from 3 new mock data files
5. No existing types or functions modified -- only additions
</verification>

<success_criteria>
- 4 new TypeScript interfaces in types.ts following French field name convention
- 3 mock data JSON files with realistic WildWood data
- 3 new async getter functions in data-access.ts
- Zero type errors, successful build
</success_criteria>

<output>
After completion, create `.planning/phases/06-secondary-modules/06-01-SUMMARY.md`
</output>
