---
phase: 02-pos-cash-register
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 5 phase success criteria are verified by automated checks"
    - "Visual inspection confirms tablet-friendly layout with no scrolling"
    - "Full checkout flows tested end-to-end on both gym pass and F&B paths"
  artifacts: []
  key_links: []
---

<objective>
Run automated verification of all Phase 2 success criteria and perform visual checkpoint to confirm tablet-ready POS interface.

Purpose: Ensures all 8 requirements (POS-01 through POS-08) are met and the interface works as expected before moving to Phase 3.
Output: VERIFICATION.md confirming phase completion or identifying gaps.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pos-cash-register/02-RESEARCH.md
@src/components/pos/pos-register.tsx
@src/components/pos/product-grid.tsx
@src/components/pos/gym-pass-grid.tsx
@src/components/pos/fnb-grid.tsx
@src/components/pos/client-popup.tsx
@src/components/pos/cart-sidebar.tsx
@src/contexts/transactions-context.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of Phase 2 requirements</name>
  <files></files>
  <action>
    Run the following checks and record results:

    **Build check:**
    - `pnpm build` -- must pass with zero errors

    **File existence checks:**
    - All expected files exist in src/components/pos/ (pos-register.tsx, product-grid.tsx, gym-pass-grid.tsx, fnb-grid.tsx, client-popup.tsx, cart-sidebar.tsx)
    - src/contexts/transactions-context.tsx exists
    - All 6 shadcn components exist in src/components/ui/ (dialog, tabs, input, label, sonner, scroll-area)

    **Code content checks (grep verification):**

    POS-01 (Gym pass grid):
    - grep for "grid-cols-3" in gym-pass-grid.tsx (3x3 grid)
    - grep for 'variant="pos"' and 'size="pos"' in gym-pass-grid.tsx (120x80px touch targets)
    - grep for "toLocaleString" in gym-pass-grid.tsx (formatted prices)

    POS-02 (Client popup):
    - grep for "DialogContent" in client-popup.tsx (Dialog is used)
    - grep for "prenom" AND "nom" AND "email" AND "telephone" in client-popup.tsx (all 4 fields)

    POS-03 (Client detection):
    - grep for "toLowerCase" in client-popup.tsx (email normalization)
    - grep for "replace" in client-popup.tsx (phone normalization)

    POS-04 (Bungalow resident):
    - grep for "en-cours" in client-popup.tsx (active reservation check)
    - grep for "Resident Bungalow" in client-popup.tsx OR cart-sidebar.tsx (badge display)
    - grep for "isBungalowResident" in pos-register.tsx (state tracked)

    POS-05 (F&B categories):
    - grep for "bowls" AND "cocktails-proteines" AND "cafes" AND "smoothies" AND "boissons" AND "snacks" in fnb-grid.tsx (all 6 categories)
    - grep for "activeCategory" in fnb-grid.tsx (filtering works)

    POS-06 (Cart with total):
    - grep for "Panier" in cart-sidebar.tsx (cart title)
    - grep for "total" in cart-sidebar.tsx (running total)
    - grep for "ScrollArea" in cart-sidebar.tsx (scrollable items list)

    POS-07 (Encaisser button):
    - grep for "Encaisser" in cart-sidebar.tsx (button exists)
    - grep for "toast.success" in pos-register.tsx (confirmation feedback)

    POS-08 (Transaction in context):
    - grep for "addTransaction" in pos-register.tsx (transaction stored)
    - grep for "TransactionsProvider" in layout.tsx (context provided)

    **Server/Client boundary check:**
    - page.tsx must NOT have 'use client' (Server Component)
    - pos-register.tsx MUST have 'use client' (Client Component)

    Write results to `.planning/phases/02-pos-cash-register/02-VERIFICATION.md` with pass/fail for each requirement.
  </action>
  <verify>VERIFICATION.md file exists and contains results for all 8 requirements</verify>
  <done>Automated verification complete with pass/fail for POS-01 through POS-08.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete POS cash register interface with:
    - 9 gym pass buttons in a 3x3 grid (Passes Gym tab)
    - F&B products organized by 6 categories (F&B tab)
    - Client popup with existing client detection and bungalow resident badge
    - Cart sidebar with item management, running total, and payment method toggle
    - Encaisser checkout with sonner toast confirmation
    - In-memory transaction storage
  </what-built>
  <how-to-verify>
    Start the dev server (`pnpm dev`) and navigate to http://localhost:3000/pos

    **1. Gym Pass Grid (POS-01):**
    - Verify 9 gym pass buttons visible in 3x3 grid
    - Verify prices shown: 350, 800, 1,200, 1,400, 2,000, 9,000, 15,000, 250, 200 THB
    - Verify all buttons are at least 120x80px (large touch targets)
    - Verify dark POS theme (dark background, light text)
    - Verify NO scrolling required

    **2. Client Popup (POS-02, POS-03):**
    - Tap any gym pass (e.g., "1 jour")
    - Verify popup opens with "Client pour 1 jour" title
    - Verify 4 fields: Prenom, Nom, Email, Telephone
    - Test existing client detection: type an email from mock data and verify auto-fill
    - Test "Sans client" option -- popup closes, pass added to cart

    **3. Bungalow Resident (POS-04):**
    - Tap a gym pass, enter a resident client's email (one with active 'en-cours' reservation)
    - Verify "Resident Bungalow" badge appears
    - Confirm, then check cart shows 0 THB for the gym pass

    **4. F&B Tab (POS-05):**
    - Switch to F&B tab
    - Verify 6 category filter buttons visible
    - Tap through each category -- products change accordingly
    - Verify products show emoji, name, and price

    **5. Cart and Checkout (POS-06, POS-07, POS-08):**
    - Add several F&B items to cart
    - Verify running total updates
    - Test quantity +/- buttons
    - Test remove item
    - Toggle payment method (Especes/Virement)
    - Tap "Encaisser"
    - Verify success toast appears ("Transaction enregistree")
    - Verify cart clears after checkout

    **6. Overall UX:**
    - Everything fits on screen without scrolling (except cart items list if many)
    - 3-tap flow works: tap product -> (optional client) -> Encaisser
    - Dark theme consistent throughout (including dialog popup)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 8 POS requirements (POS-01 through POS-08) pass automated grep checks and visual human verification.
</verification>

<success_criteria>
1. VERIFICATION.md shows all POS-01 through POS-08 as PASS
2. Human visual inspection confirms tablet-ready layout
3. Full checkout flows work end-to-end for both gym pass and F&B paths
4. Build passes clean with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-pos-cash-register/02-04-SUMMARY.md`
</output>
