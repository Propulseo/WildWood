---
phase: 02-pos-cash-register
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/pos/gym-pass-grid.tsx
  - src/components/pos/fnb-grid.tsx
  - src/components/pos/product-grid.tsx
  - src/components/pos/pos-register.tsx
autonomous: true

must_haves:
  truths:
    - "User sees a 3x3 grid of 9 gym pass buttons with name and price in THB on each button"
    - "Each gym pass button is at least 120x80px using the existing pos button variant"
    - "User can switch between Passes Gym and F&B tabs"
    - "F&B tab shows 6 category filter buttons (bowls, cocktails proteines, cafes, smoothies, boissons, snacks)"
    - "Selecting an F&B category shows only products in that category as a grid of buttons with emoji, name, and price"
    - "Tapping an F&B product button adds it to the cart (dispatches ADD_ITEM)"
  artifacts:
    - path: "src/components/pos/gym-pass-grid.tsx"
      provides: "3x3 grid of gym pass buttons"
      contains: "variant=\"pos\""
      min_lines: 20
    - path: "src/components/pos/fnb-grid.tsx"
      provides: "F&B product grid with category filtering"
      contains: "categorie"
      min_lines: 40
    - path: "src/components/pos/product-grid.tsx"
      provides: "Tabbed container (Passes Gym / F&B) wrapping both grids"
      contains: "Tabs"
      min_lines: 20
  key_links:
    - from: "src/components/pos/product-grid.tsx"
      to: "src/components/pos/gym-pass-grid.tsx"
      via: "import and render inside Passes Gym tab"
      pattern: "<GymPassGrid"
    - from: "src/components/pos/product-grid.tsx"
      to: "src/components/pos/fnb-grid.tsx"
      via: "import and render inside F&B tab"
      pattern: "<FnbGrid"
    - from: "src/components/pos/pos-register.tsx"
      to: "src/components/pos/product-grid.tsx"
      via: "render in left panel of two-column grid"
      pattern: "<ProductGrid"
---

<objective>
Build the product display side of the POS register: a tabbed interface with Passes Gym (3x3 grid of 9 gym pass buttons) and F&B (6-category filter with product grids), all using the existing pos button variants and dark POS theme.

Purpose: This is the core interaction surface -- the staff taps products here. Without the product grids, there is nothing to sell.
Output: Fully populated product grids for both gym passes and F&B, switchable via tabs, wired into the PosRegister layout.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pos-cash-register/02-RESEARCH.md
@src/lib/types.ts
@src/lib/mock-data/gym-passes.json
@src/lib/mock-data/fnb-products.json
@src/components/ui/button.tsx
@src/components/ui/tabs.tsx
@src/components/pos/pos-register.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GymPassGrid and FnbGrid components</name>
  <files>
    src/components/pos/gym-pass-grid.tsx
    src/components/pos/fnb-grid.tsx
  </files>
  <action>
    **1. Create `src/components/pos/gym-pass-grid.tsx`:**

    Props:
    ```typescript
    interface GymPassGridProps {
      gymPasses: GymPass[]
      onSelectPass: (pass: GymPass) => void
    }
    ```

    Renders a 3x3 grid of gym pass buttons:
    - Grid layout: `grid grid-cols-3 gap-3 p-4`
    - Each button uses `<Button variant="pos" size="pos">` (existing variants from button.tsx)
    - Button content is a flex column: pass name (font-display, uppercase, text-base) on top, price in THB below (text-wildwood-orange, font-bold)
    - Format price with `.toLocaleString()` for thousand separators: e.g., "15,000 THB"
    - onClick calls `onSelectPass(pass)` -- this will trigger the client popup (wired in Plan 03)
    - No 'use client' needed -- this is a child of the already-client PosRegister

    There are exactly 9 gym passes in mock data, so 3x3 fills perfectly with no gaps.

    **2. Create `src/components/pos/fnb-grid.tsx`:**

    Props:
    ```typescript
    interface FnbGridProps {
      fnbProducts: FnbProduct[]
      onAddItem: (product: FnbProduct) => void
    }
    ```

    This component has TWO parts:

    A) Category filter row at the top:
    - Define the 6 categories as a constant array:
      ```typescript
      const FNB_CATEGORIES = [
        { key: 'bowls', label: 'Bowls' },
        { key: 'cocktails-proteines', label: 'Cocktails' },
        { key: 'cafes', label: 'Cafes' },
        { key: 'smoothies', label: 'Smoothies' },
        { key: 'boissons', label: 'Boissons' },
        { key: 'snacks', label: 'Snacks' },
      ] as const
      ```
    - Use useState for activeCategory, defaulting to 'bowls'
    - Render as a row of small buttons (NOT full shadcn Tabs -- too heavy for sub-nav):
      - Use `<Button variant={isActive ? "pos-accent" : "pos"} size="sm">` for each category
      - Active state uses pos-accent (orange), inactive uses pos (bois)
      - Row layout: `flex gap-2 p-4 pb-0 flex-wrap`

    B) Product grid below the filter:
    - Filter products: `fnbProducts.filter(p => p.categorie === activeCategory)`
    - Grid layout: `grid grid-cols-3 gap-3 p-4` (same as gym passes for consistency)
    - Each button uses `<Button variant="pos" size="pos">`
    - Button content is a flex column: emoji (text-2xl), product name (text-sm, font-medium), price in THB (text-wildwood-orange, text-xs, font-bold)
    - onClick calls `onAddItem(product)` which will dispatch ADD_ITEM to the cart reducer

    IMPORTANT:
    - FnbGrid needs 'use client' because it uses useState for activeCategory (OR the parent can be client and this can stay without -- since PosRegister is already 'use client', child components inherit client context. However, useState requires the file to be a client component OR be used in one). Since this component uses `useState` directly, do NOT add 'use client' -- it will be imported by pos-register.tsx which is already 'use client'. Components without 'use client' that use hooks work fine when rendered within a client component tree.
    - Actually, to be safe and explicit: Do NOT add 'use client' to these files. They use hooks (useState in FnbGrid) but are rendered inside the PosRegister client boundary. This works because the client boundary propagates to all children.
  </action>
  <verify>
    - `grep -q "grid-cols-3" src/components/pos/gym-pass-grid.tsx` -- 3-column grid
    - `grep -q "variant=\"pos\"" src/components/pos/gym-pass-grid.tsx` -- uses pos button variant
    - `grep -q "activeCategory" src/components/pos/fnb-grid.tsx` -- category filtering exists
    - `grep -q "FNB_CATEGORIES" src/components/pos/fnb-grid.tsx` -- 6 categories defined
    - `pnpm build` passes
  </verify>
  <done>GymPassGrid renders 9 buttons in 3x3 grid with name+price. FnbGrid renders category filter row (6 categories) and filtered product grid with emoji+name+price. Both use existing pos button variants.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProductGrid tabbed container and wire into PosRegister</name>
  <files>
    src/components/pos/product-grid.tsx
    src/components/pos/pos-register.tsx
  </files>
  <action>
    **1. Create `src/components/pos/product-grid.tsx`:**

    Props:
    ```typescript
    interface ProductGridProps {
      gymPasses: GymPass[]
      fnbProducts: FnbProduct[]
      onSelectGymPass: (pass: GymPass) => void
      onAddFnbItem: (product: FnbProduct) => void
    }
    ```

    Uses shadcn Tabs component to switch between "Passes Gym" and "F&B":
    - Import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
    - Default tab: "gym" (gym passes are primary product)
    - TabsList should be styled for the POS theme: full-width, large touch targets
      - `<TabsList className="w-full h-12 grid grid-cols-2 bg-secondary">` to make tabs fill the width equally
      - Each TabsTrigger: `className="text-base font-display uppercase data-[state=active]:bg-wildwood-orange data-[state=active]:text-white"` for clear active state
    - Tab values: "gym" and "fnb"
    - TabsTrigger labels: "Passes Gym" and "F&B"
    - Each TabsContent renders the respective grid component:
      - "gym" tab: `<GymPassGrid gymPasses={gymPasses} onSelectPass={onSelectGymPass} />`
      - "fnb" tab: `<FnbGrid fnbProducts={fnbProducts} onAddItem={onAddFnbItem} />`
    - TabsContent should use `className="flex-1 overflow-hidden"` to prevent overflow
    - The entire Tabs component should be `className="flex flex-col h-full"` to fill the left panel

    **2. Update `src/components/pos/pos-register.tsx`:**

    Replace the left panel placeholder with the ProductGrid:
    - Import ProductGrid from './product-grid'
    - Import GymPass, FnbProduct types from @/lib/types
    - Create callback functions:
      - `handleSelectGymPass`: For now, just stores the selected pass in a useState (the client popup will be wired in Plan 03). Add `const [selectedPass, setSelectedPass] = useState<GymPass | null>(null)` and `const [clientDialogOpen, setClientDialogOpen] = useState(false)`. The handler sets both: `setSelectedPass(pass); setClientDialogOpen(true)`.
      - `handleAddFnbItem`: Dispatches ADD_ITEM to cart reducer with the F&B product info:
        ```typescript
        dispatch({
          type: 'ADD_ITEM',
          payload: {
            produitId: product.id,
            nom: product.nom,
            prixUnitaire: product.prix,
            type: 'fnb',
          },
        })
        ```

    Replace left panel content:
    ```tsx
    <div className="flex flex-col overflow-hidden border-r border-border">
      <ProductGrid
        gymPasses={gymPasses}
        fnbProducts={fnbProducts}
        onSelectGymPass={handleSelectGymPass}
        onAddFnbItem={handleAddFnbItem}
      />
    </div>
    ```

    Also export the selectedPass and clientDialogOpen state so Plan 03 can wire the client popup. Actually, since Plan 03 will edit this file too, just leave the state variables in place -- Plan 03 will add the Dialog that consumes them.

    Keep the right panel placeholder ("Cart sidebar coming in Plan 03") for now.
  </action>
  <verify>
    - `grep -q "Tabs" src/components/pos/product-grid.tsx` -- uses shadcn Tabs
    - `grep -q "ProductGrid" src/components/pos/pos-register.tsx` -- ProductGrid wired in
    - `grep -q "handleAddFnbItem" src/components/pos/pos-register.tsx` -- F&B dispatch handler exists
    - `grep -q "clientDialogOpen" src/components/pos/pos-register.tsx` -- dialog state prepared
    - `pnpm build` passes
    - Dev server at /pos shows tabbed product grids with real data (9 gym passes, F&B with category filter)
  </verify>
  <done>ProductGrid renders with Passes Gym (9 buttons, 3x3) and F&B tabs (6 categories, filtered product grid). Gym pass taps set selectedPass state and open dialog flag. F&B taps dispatch ADD_ITEM to cart. All wired into PosRegister left panel. Build clean.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero errors
- /pos route shows two tabs: "Passes Gym" (active by default) and "F&B"
- Gym tab displays 9 buttons in 3x3 grid, each showing pass name and price (350 to 15,000 THB)
- Switching to F&B tab shows 6 category buttons, with "Bowls" active by default showing 3 bowl products
- Each category filter shows the correct products (e.g., Cafes shows 4 items: Espresso, Americano, Latte, Cappuccino)
- All buttons meet 120x80px minimum size (pos variant + pos size)
- No scrolling on the main POS area
</verification>

<success_criteria>
1. Nine gym pass buttons render in 3x3 grid with correct names and prices from mock data
2. Tabs switch between "Passes Gym" and "F&B" views
3. F&B has 6 category filter buttons, default to "Bowls", filtering works correctly
4. F&B product buttons show emoji, name, and price
5. Tapping F&B product dispatches ADD_ITEM to cart reducer
6. Gym pass tap sets selectedPass state (client popup wired in Plan 03)
7. No scroll overflow, all content fits within POS layout constraints
8. Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-pos-cash-register/02-02-SUMMARY.md`
</output>
