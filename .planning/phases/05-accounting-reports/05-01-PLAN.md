---
phase: 05-accounting-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/mock-data/expenses.json
  - src/lib/data-access.ts
  - src/contexts/expenses-context.tsx
  - src/app/(admin)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Expense type exists with categorie, montant, date, note fields"
    - "Mock expenses cover multiple categories and months for chart/table data"
    - "ExpensesContext provides expenses array and addExpense function"
    - "ExpensesContext is available to all admin pages"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Expense interface"
      contains: "interface Expense"
    - path: "src/lib/mock-data/expenses.json"
      provides: "Realistic mock expense data"
      min_lines: 30
    - path: "src/lib/data-access.ts"
      provides: "getExpenses function"
      exports: ["getExpenses"]
    - path: "src/contexts/expenses-context.tsx"
      provides: "Expenses context with add capability"
      exports: ["ExpensesProvider", "useExpenses"]
  key_links:
    - from: "src/contexts/expenses-context.tsx"
      to: "src/lib/data-access.ts"
      via: "getExpenses import"
      pattern: "import.*getExpenses.*from.*data-access"
    - from: "src/app/(admin)/layout.tsx"
      to: "src/contexts/expenses-context.tsx"
      via: "ExpensesProvider wrapping children"
      pattern: "ExpensesProvider"
---

<objective>
Create the Expense data layer: type definition, mock data, data-access function, and React context with provider wired into the admin layout.

Purpose: All comptabilite views need expense data. This plan establishes the foundation so Plan 02 can focus purely on UI.
Output: Expense type, ~20 mock expenses spanning multiple months/categories, ExpensesContext available to all admin children.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/types.ts
@src/lib/data-access.ts
@src/contexts/transactions-context.tsx
@src/app/(admin)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Expense type, mock data, and data-access function</name>
  <files>
    src/lib/types.ts
    src/lib/mock-data/expenses.json
    src/lib/data-access.ts
  </files>
  <action>
    1. In src/lib/types.ts, add at the end (before the Role type):

    ```typescript
    /** Depense manuelle saisie par l'owner */
    export interface Expense {
      id: string
      /** Categorie de depense */
      categorie: 'Fournitures' | 'Salaires' | 'Maintenance' | 'Marketing' | 'Divers'
      /** Montant en baht thailandais */
      montant: number
      /** Date de la depense -- format ISO YYYY-MM-DD */
      date: string
      /** Note explicative optionnelle */
      note?: string
    }
    ```

    ID convention: `exp-XXX` (following project pattern from 01-02 decision).

    2. Create src/lib/mock-data/expenses.json with ~20 realistic mock expenses:
    - Spread across the last 12 months (use 2026 dates, Jan through Feb 2026)
    - Cover all 5 categories: Fournitures (gym equipment, cleaning supplies), Salaires (monthly staff pay), Maintenance (bungalow repairs, pool), Marketing (social media ads, flyers), Divers (miscellaneous)
    - Amounts realistic for Thai resort: Salaires 15000-25000 THB, Fournitures 2000-8000 THB, Maintenance 3000-15000 THB, Marketing 1000-5000 THB, Divers 500-3000 THB
    - Include French notes (e.g., "Achat serviettes et tapis", "Salaire Nong mars", "Reparation toit bungalow 3")
    - At least 2-3 expenses for today's date (2026-03-01) so the daily view has data
    - At least 3-4 expenses per month for recent months so monthly/annual charts look good

    3. In src/lib/data-access.ts:
    - Add `import type { Expense }` to the existing import (add Expense to the destructured import)
    - Add `import expensesData from './mock-data/expenses.json'`
    - Add function:
    ```typescript
    export async function getExpenses(): Promise<Expense[]> {
      return expensesData as Expense[]
    }
    ```
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Verify expenses.json is valid JSON with ~20 entries.</verify>
  <done>Expense type exported from types.ts, expenses.json has ~20 realistic entries spanning 12 months with all 5 categories, getExpenses available in data-access.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Create ExpensesContext and wire into admin layout</name>
  <files>
    src/contexts/expenses-context.tsx
    src/app/(admin)/layout.tsx
  </files>
  <action>
    1. Create src/contexts/expenses-context.tsx following the EXACT same pattern as transactions-context.tsx:
    - 'use client' directive
    - Import createContext, useContext, useState, useEffect, type ReactNode from react
    - Import Expense type from @/lib/types
    - Import getExpenses from @/lib/data-access
    - Interface ExpensesContextType with `expenses: Expense[]` and `addExpense: (exp: Expense) => void`
    - createContext with null default (React 19 syntax: `<ExpensesContext value={...}>` NOT `.Provider`)
    - ExpensesProvider component: useState for expenses array, useEffect to load via getExpenses().then(setExpenses), addExpense prepends new expense
    - useExpenses hook with null-check error throw
    - Export ExpensesProvider and useExpenses

    2. In src/app/(admin)/layout.tsx:
    - Add import: `import { ExpensesProvider } from '@/contexts/expenses-context'`
    - Wrap the existing TransactionsProvider + children with ExpensesProvider. The nesting order should be:
      ```tsx
      <ExpensesProvider>
        <TransactionsProvider>{children}</TransactionsProvider>
      </ExpensesProvider>
      ```
    - Do NOT change anything else in the layout file.
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Run `npm run build` -- build succeeds. Check that layout.tsx has both providers nested.</verify>
  <done>ExpensesContext provides expenses array and addExpense function. ExpensesProvider wraps admin layout children alongside TransactionsProvider. Any admin page can call useExpenses().</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds
- expenses.json has ~20 entries with all 5 categories
- ExpensesProvider appears in admin layout.tsx wrapping children
</verification>

<success_criteria>
- Expense type is exported from src/lib/types.ts
- Mock expenses span 12 months with 5 categories and realistic amounts
- getExpenses is available in data-access.ts (sole importer of mock-data/)
- useExpenses() hook is callable from any admin page
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-accounting-reports/05-01-SUMMARY.md`
</output>
