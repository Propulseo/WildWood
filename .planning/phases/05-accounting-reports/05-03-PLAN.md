---
phase: 05-accounting-reports
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Daily view shows today's total revenue broken down by 3 revenue centers plus today's expenses"
    - "Monthly view displays a Recharts bar chart of revenue vs expenses per month, with a recap table and net balance"
    - "Annual view shows a 12-month table with totals for each revenue center"
    - "User can manually enter an expense with category, amount, date, and note"
  artifacts: []
  key_links: []
---

<objective>
Verify Phase 5 completion: all accounting views render correctly and expense entry works.

Purpose: Automated build verification + visual checkpoint for the owner to confirm the comptabilite page meets expectations.
Output: Verified phase or list of issues to fix.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-accounting-reports/05-01-SUMMARY.md
@.planning/phases/05-accounting-reports/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated build and type verification</name>
  <files></files>
  <action>
    Run the following checks in sequence:
    1. `npx tsc --noEmit` -- verify zero type errors
    2. `npm run build` -- verify production build succeeds
    3. Verify file existence:
       - src/lib/types.ts contains "interface Expense"
       - src/lib/mock-data/expenses.json exists and has entries
       - src/lib/data-access.ts contains "getExpenses"
       - src/contexts/expenses-context.tsx exists
       - src/app/(admin)/comptabilite/page.tsx exists
       - src/components/comptabilite/vue-journaliere.tsx exists
       - src/components/comptabilite/vue-mensuelle.tsx exists
       - src/components/comptabilite/vue-annuelle.tsx exists
       - src/components/comptabilite/depense-dialog.tsx exists
    4. Verify key wiring:
       - admin layout.tsx imports ExpensesProvider
       - comptabilite/page.tsx imports all 3 view components
       - vue-mensuelle.tsx imports from recharts
       - depense-dialog.tsx calls addExpense

    If any check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>All commands exit 0. All file checks pass.</verify>
  <done>Build passes, types check, all files exist, key wiring verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete accounting and reports module with three views (daily, monthly, annual) and expense entry.</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000/comptabilite (start dev server with `npm run dev` if not running)
    2. DAILY VIEW (default tab "Journaliere"):
       - Verify total revenue for today appears with THB formatting
       - Verify 3 revenue center cards: Gym (orange), F&B (lime/green), Bungalows (brown)
       - Verify today's expenses section shows expense entries or "no expenses" message
       - Verify net balance card shows revenue minus expenses
    3. MONTHLY VIEW (click "Mensuelle" tab):
       - Verify a Recharts bar chart appears with green revenue bars and red expense bars
       - Verify X-axis shows month abbreviations, Y-axis shows THB amounts
       - Verify recap table below chart has Mois/Revenus/Depenses/Solde Net columns
       - Verify total row at bottom
    4. ANNUAL VIEW (click "Annuelle" tab):
       - Verify 12-month table with columns: Mois, Gym, F&B, Bungalows, Total
       - Verify brand colors on revenue center columns
       - Verify footer row with annual totals
    5. EXPENSE ENTRY:
       - Click "Ajouter une depense" button
       - Fill: Categorie=Fournitures, Montant=5000, Date=today, Note="Test depense"
       - Click "Enregistrer"
       - Verify new expense appears in the daily view expenses list
       - Verify daily net balance updated to reflect the new expense
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Build passes
- All 3 tab views render with correct data
- Expense entry works end-to-end
- Visual design matches WildWood brand (orange/lime/bois colors, Oswald headings)
</verification>

<success_criteria>
- Phase 5 requirements COMPT-01 through COMPT-06 are all satisfied
- User confirms visual and functional correctness
</success_criteria>

<output>
After completion, create `.planning/phases/05-accounting-reports/05-03-SUMMARY.md`
</output>
