---
phase: 05-accounting-reports
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(admin)/comptabilite/page.tsx
  - src/components/comptabilite/vue-journaliere.tsx
  - src/components/comptabilite/vue-mensuelle.tsx
  - src/components/comptabilite/vue-annuelle.tsx
  - src/components/comptabilite/depense-dialog.tsx
autonomous: true

must_haves:
  truths:
    - "Daily view shows today's total revenue broken down by 3 revenue centers plus today's expenses"
    - "Monthly view displays a Recharts bar chart of revenue vs expenses per month with recap table and net balance"
    - "Annual view shows a 12-month table with totals for each revenue center"
    - "User can manually enter an expense with category, amount, date, and note"
  artifacts:
    - path: "src/app/(admin)/comptabilite/page.tsx"
      provides: "Tab-based navigation shell for daily/monthly/annual views"
      min_lines: 30
    - path: "src/components/comptabilite/vue-journaliere.tsx"
      provides: "Daily revenue breakdown by centre + expenses list"
      min_lines: 40
    - path: "src/components/comptabilite/vue-mensuelle.tsx"
      provides: "Monthly Recharts bar chart + recap table + net balance"
      min_lines: 60
    - path: "src/components/comptabilite/vue-annuelle.tsx"
      provides: "12-month table with revenue center columns and totals"
      min_lines: 50
    - path: "src/components/comptabilite/depense-dialog.tsx"
      provides: "Expense entry form in dialog"
      min_lines: 40
  key_links:
    - from: "src/app/(admin)/comptabilite/page.tsx"
      to: "src/components/comptabilite/vue-journaliere.tsx"
      via: "import and render in Tabs"
      pattern: "import.*VueJournaliere"
    - from: "src/components/comptabilite/vue-journaliere.tsx"
      to: "src/contexts/transactions-context.tsx"
      via: "useTransactions hook"
      pattern: "useTransactions"
    - from: "src/components/comptabilite/vue-journaliere.tsx"
      to: "src/contexts/expenses-context.tsx"
      via: "useExpenses hook"
      pattern: "useExpenses"
    - from: "src/components/comptabilite/vue-mensuelle.tsx"
      to: "recharts"
      via: "BarChart import"
      pattern: "import.*BarChart.*from.*recharts"
    - from: "src/components/comptabilite/depense-dialog.tsx"
      to: "src/contexts/expenses-context.tsx"
      via: "addExpense from useExpenses"
      pattern: "addExpense"
---

<objective>
Build the complete comptabilite page with three tab views (daily, monthly, annual) and an expense entry dialog.

Purpose: This is the core accounting interface -- the owner needs to answer "how much did I make?" at three time scales and track expenses.
Output: Fully functional /comptabilite page with shadcn Tabs, Recharts bar chart, data tables, and expense form.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-accounting-reports/05-01-SUMMARY.md
@src/lib/types.ts
@src/contexts/transactions-context.tsx
@src/contexts/expenses-context.tsx
@src/app/(admin)/dashboard/page.tsx
@src/components/ui/tabs.tsx
@src/components/ui/card.tsx
@src/components/ui/dialog.tsx
@src/components/ui/table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comptabilite page shell + Daily view component</name>
  <files>
    src/app/(admin)/comptabilite/page.tsx
    src/components/comptabilite/vue-journaliere.tsx
  </files>
  <action>
    1. Create src/app/(admin)/comptabilite/page.tsx:
    - 'use client' directive
    - Import shadcn Tabs, TabsList, TabsTrigger, TabsContent from @/components/ui/tabs
    - Import VueJournaliere, VueMensuelle, VueAnnuelle from @/components/comptabilite/
    - Import DepenseDialog from @/components/comptabilite/depense-dialog
    - Page heading: "Comptabilite" (font-display text-3xl font-bold) with subtitle "Suivi des revenus et depenses"
    - Below heading, right-aligned: DepenseDialog button (for adding expenses from any tab)
    - Tabs component with defaultValue="journaliere" and three triggers:
      - "Journaliere" (daily)
      - "Mensuelle" (monthly)
      - "Annuelle" (annual)
    - Each TabsContent renders the corresponding component

    2. Create src/components/comptabilite/vue-journaliere.tsx:
    - 'use client' directive
    - Import useTransactions and useExpenses hooks
    - Import useMemo from react, isToday and parseISO from date-fns
    - Import Card, CardContent, CardHeader, CardTitle from shadcn
    - Import Table components from shadcn for expenses list

    Compute in useMemo (filtering for today):
    - todayRevenue: total from today's transactions
    - revenueByCenter: { Gym: number, 'F&B': number, Bungalows: number } (same pattern as dashboard)
    - todayExpenses: expenses where isToday(parseISO(exp.date))
    - totalExpenses: sum of today's expenses

    Render:
    - Top summary card: "Revenu du jour: {total} THB" (bold, large) and "Depenses du jour: {total} THB" in red
    - "Solde net: {revenue - expenses} THB" card with green if positive, red if negative
    - Revenue breakdown: 3 cards in a grid (Gym orange, F&B lime, Bungalows bois) -- same brand colors as dashboard
    - Below: "Depenses du jour" section with a Table showing each expense (categorie, montant, note) or "Aucune depense aujourd'hui" if empty

    Use WildWood brand colors: text-wildwood-orange (Gym), text-wildwood-lime (F&B), text-wildwood-bois (Bungalows).
    Use font-display for numeric values. Format amounts with .toLocaleString() + " THB".
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Navigate to /comptabilite in browser -- tabs render, daily view shows revenue breakdown and expenses.</verify>
  <done>Comptabilite page renders with 3 tabs. Daily tab shows today's revenue by center, today's expenses, and net balance.</done>
</task>

<task type="auto">
  <name>Task 2: Monthly Recharts bar chart + Annual 12-month table</name>
  <files>
    src/components/comptabilite/vue-mensuelle.tsx
    src/components/comptabilite/vue-annuelle.tsx
  </files>
  <action>
    1. Create src/components/comptabilite/vue-mensuelle.tsx:
    - 'use client' directive
    - Import useTransactions and useExpenses hooks
    - Import useMemo from react
    - Import parseISO, format, startOfYear, eachMonthOfInterval, endOfYear, isSameMonth from date-fns
    - Import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from 'recharts'
    - Import Card, Table components from shadcn

    Compute in useMemo:
    - Get current year (2026)
    - Generate 12 months array using eachMonthOfInterval({ start: startOfYear(now), end: endOfYear(now) })
    - For each month, compute:
      - revenus: sum of transactions where isSameMonth(parseISO(txn.date), month)
      - depenses: sum of expenses where isSameMonth(parseISO(exp.date), month)
    - Build chartData array: [{ mois: 'Jan', revenus: number, depenses: number }, ...]
    - Use format(month, 'MMM') for month labels (will be English abbreviations, that's fine)

    Render:
    - ResponsiveContainer width="100%" height={400}
    - BarChart with chartData
    - CartesianGrid strokeDasharray="3 3"
    - XAxis dataKey="mois"
    - YAxis with tickFormatter to add " THB" suffix
    - Tooltip formatter to add " THB"
    - Two Bar elements:
      - revenus: fill with a green/lime color (use CSS variable or hardcode oklch matching wildwood-lime)
      - depenses: fill with a red/orange color (use a muted red like #e74c3c)
    - Legend with French labels
    - Below the chart: a recap Table with columns Mois | Revenus | Depenses | Solde Net
    - Each row shows the month data, solde net = revenus - depenses
    - Color solde net green if positive, red if negative
    - Final row: TOTAL with bold sums

    IMPORTANT for Recharts v3 with Next.js: The recharts components need 'use client'. Since this component already has 'use client', that's sufficient. Do NOT use dynamic import for recharts -- direct imports work fine with 'use client'.

    2. Create src/components/comptabilite/vue-annuelle.tsx:
    - 'use client' directive
    - Import useTransactions from transactions-context
    - Import useMemo from react
    - Import parseISO, getMonth, getYear from date-fns
    - Import Card, Table components from shadcn

    Compute in useMemo:
    - Filter transactions for current year (2026)
    - Build a 12-row array, one per month (Jan=0 through Dec=11)
    - For each month, compute revenue by centre: Gym, F&B, Bungalows, and total
    - Month names in French: ['Janvier', 'Fevrier', 'Mars', 'Avril', 'Mai', 'Juin', 'Juillet', 'Aout', 'Septembre', 'Octobre', 'Novembre', 'Decembre']

    Render:
    - Card wrapping a Table
    - Table headers: Mois | Gym | F&B | Bungalows | Total
    - 12 rows, one per month
    - Gym values in text-wildwood-orange, F&B in text-wildwood-lime, Bungalows in text-wildwood-bois
    - Total column in bold
    - Footer row: annual totals per column, bold and larger
    - Format all amounts with .toLocaleString() + " THB"
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Navigate to /comptabilite, click "Mensuelle" tab -- bar chart renders with colored bars. Click "Annuelle" tab -- 12-month table renders with brand colors.</verify>
  <done>Monthly view shows Recharts bar chart with revenue vs expenses + recap table with net balance. Annual view shows 12-month table with per-center breakdown and totals.</done>
</task>

<task type="auto">
  <name>Task 3: Expense entry dialog</name>
  <files>
    src/components/comptabilite/depense-dialog.tsx
  </files>
  <action>
    Create src/components/comptabilite/depense-dialog.tsx:
    - 'use client' directive
    - Import useState from react
    - Import useExpenses from @/contexts/expenses-context
    - Import Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger, DialogFooter, DialogClose from shadcn dialog
    - Import Button from shadcn button
    - Import Input, Label from shadcn
    - Import Select, SelectContent, SelectItem, SelectTrigger, SelectValue from shadcn select
    - Import Plus icon from lucide-react

    Component DepenseDialog (default export):
    - State: open (boolean for dialog), categorie (string), montant (string), date (string, default to today ISO), note (string)
    - The trigger button: Button variant="outline" with Plus icon and text "Ajouter une depense"

    Dialog content:
    - Title: "Nouvelle depense"
    - Form fields (not a form element, just controlled inputs):
      1. Categorie: Select with 5 options -- Fournitures, Salaires, Maintenance, Marketing, Divers
      2. Montant: Input type="number" placeholder="Montant en THB", min="0"
      3. Date: Input type="date" defaulting to today (format YYYY-MM-DD)
      4. Note: Input (not textarea -- keep it simple) placeholder="Note optionnelle"
    - Each field wrapped in div with Label above
    - Footer: "Annuler" button (DialogClose) and "Enregistrer" Button (primary)

    On submit (Enregistrer click):
    - Validate: categorie must be selected, montant must be > 0
    - If invalid, do nothing (button can be disabled when invalid)
    - Generate ID: `exp-${Date.now()}` (simple unique ID for prototype)
    - Call addExpense with: { id, categorie, montant: Number(montant), date, note: note || undefined }
    - Reset form state and close dialog (setOpen(false))

    The Enregistrer button should be disabled when categorie is empty or montant is empty/zero.
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Run `npm run build` -- build succeeds. Navigate to /comptabilite, click "Ajouter une depense", fill form, submit -- expense appears in daily view.</verify>
  <done>User can open expense dialog, fill in category/amount/date/note, and submit. New expense appears in the expenses list on the daily view and is reflected in monthly/annual calculations.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds
- /comptabilite page renders with 3 working tabs
- Daily view shows today's revenue by center + expenses + net balance
- Monthly view shows Recharts bar chart with revenue (green) vs expenses (red) bars
- Monthly recap table shows per-month breakdown with net balance
- Annual view shows 12-month table with Gym/F&B/Bungalows columns and totals
- Expense dialog opens, accepts input, submits, and new expense appears in views
</verification>

<success_criteria>
- All 3 tab views render correct data from TransactionsContext and ExpensesContext
- Recharts bar chart displays with colored bars and French-friendly formatting
- Annual table uses WildWood brand colors for revenue centers
- Expense entry creates a valid Expense and updates context in real time
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-accounting-reports/05-02-SUMMARY.md`
</output>
