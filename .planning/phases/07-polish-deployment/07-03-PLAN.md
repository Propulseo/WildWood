---
phase: 07-polish-deployment
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Every page displays a loading skeleton while data loads"
    - "Error states show a French-language message with a retry option"
    - "noindex meta tags are present in the root layout"
    - "A Reinitialiser les donnees button resets all in-memory state to mock defaults"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 7 success criteria are met: loading skeletons, error handling, noindex meta, and data reset functionality.

Purpose: Final verification that the prototype is demo-ready with proper polish.
Output: Verification results confirming all 4 success criteria pass.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-deployment/07-01-SUMMARY.md
@.planning/phases/07-polish-deployment/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of Phase 7 success criteria</name>
  <files></files>
  <action>
    Run the following verification checks:

    **SC1 - Loading skeletons on every page:**
    - Grep for "Skeleton" import in ALL page files under src/app/
    - Confirm these pages have skeleton UI: dashboard, clients, clients/[id], bungalows, newsletter, instagram
    - Confirm POS has loading.tsx: `ls src/app/(pos)/pos/loading.tsx`
    - Comptabilite is exempt (renders from context immediately)

    **SC2 - Error states with French message:**
    - Grep for "Quelque chose" in src/app/(admin)/error.tsx
    - Grep for "Quelque chose" in src/app/(pos)/pos/error.tsx
    - Grep for "Reessayer" (retry button) in both error files

    **SC3 - noindex meta tags:**
    - Grep for "noindex" in src/app/layout.tsx
    - Confirm robots metadata is set

    **SC4 - Data reset button:**
    - Grep for "resetTransactions" in src/contexts/transactions-context.tsx
    - Grep for "resetExpenses" in src/contexts/expenses-context.tsx
    - Grep for "Reinitialiser" in src/app/(admin)/layout.tsx

    **Build verification:**
    - Run `npx next build` and confirm 0 errors
    - Report total number of routes built

    Compile results into a verification table.
  </action>
  <verify>All 4 success criteria checks pass. Build succeeds with 0 errors.</verify>
  <done>Phase 7 verification complete. All success criteria confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 7 Polish + Deployment: Loading skeletons on all pages, French error boundaries, noindex meta, data reset button in admin sidebar.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/dashboard
    2. Verify: On initial page load, briefly visible skeleton placeholders appear before data renders (may be fast with mock data -- that's OK)
    3. Navigate to /clients, /bungalows, /instagram -- each should have skeleton structure on load
    4. Check the admin sidebar: a "Reinitialiser" button with a reset icon should appear above the collapse toggle
    5. Click "Reinitialiser" -- a toast should confirm "Donnees reinitialisees"
    6. View page source or inspect: confirm `<meta name="robots" content="noindex, nofollow" />` is present
    7. Overall: the app feels polished and demo-ready
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
All Phase 7 success criteria verified:
1. Loading skeletons on every page (no blank screens)
2. French error messages with retry
3. noindex meta tags present
4. Data reset button functional
</verification>

<success_criteria>
- Automated checks all pass
- Build succeeds with 0 errors
- User confirms visual polish is demo-ready
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deployment/07-03-SUMMARY.md`
</output>
