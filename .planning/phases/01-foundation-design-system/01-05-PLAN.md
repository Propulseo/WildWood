---
phase: 01-foundation-design-system
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can toggle between admin and staff roles on a simulated login screen, and the selected role persists across page navigation"
    - "The WildWood color palette (orange, bois, lime) is applied globally, with POS screens using a dark background and admin screens using a light sidebar layout"
    - "POS layout renders at full viewport height with no scroll, and POS buttons meet the 120x80px minimum size"
    - "Mock data files contain realistic WildWood pricing (9 gym passes, 6 F&B categories) accessible only through lib/data-access.ts"
    - "A PROTOTYPE -- Donnees fictives banner is visible on every screen"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 1 foundation works end-to-end in a browser.

Purpose: Before moving to Phase 2 (POS Cash Register), confirm that the design system, auth flow, layouts, and data layer are all working correctly. This is the quality gate for the entire foundation.
Output: Verified, working foundation ready for Phase 2 development.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
@.planning/phases/01-foundation-design-system/01-02-SUMMARY.md
@.planning/phases/01-foundation-design-system/01-03-SUMMARY.md
@.planning/phases/01-foundation-design-system/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files></files>
  <action>
  Run the following verification commands and fix any issues found:

  1. **Build check:** `npm run build` -- must pass with zero errors
  2. **Type check:** `npx tsc --noEmit` -- must pass with zero type errors
  3. **JSON validity:** Verify all 5 mock data files parse correctly:
     ```
     node -e "const c=require('./src/lib/mock-data/clients.json'); console.log('Clients:', c.length)"
     node -e "const g=require('./src/lib/mock-data/gym-passes.json'); console.log('Gym passes:', g.length)"
     node -e "const f=require('./src/lib/mock-data/fnb-products.json'); console.log('F&B products:', f.length)"
     node -e "const b=require('./src/lib/mock-data/bungalows.json'); console.log('Bungalows:', b.length)"
     node -e "const t=require('./src/lib/mock-data/transactions.json'); console.log('Transactions:', t.length)"
     ```
  4. **Data access isolation:** Verify no file other than data-access.ts imports from mock-data/:
     ```
     grep -r "mock-data" src/ --include="*.ts" --include="*.tsx" | grep -v "data-access.ts"
     ```
     This should return empty.
  5. **Gym pass pricing:** Verify exact prices match PROJECT.md:
     ```
     node -e "const g=require('./src/lib/mock-data/gym-passes.json'); g.forEach(p => console.log(p.nom, p.prix))"
     ```
     Expected: 350, 800, 1200, 1400, 2000, 9000, 15000, 250, 200
  6. **Design system check:**
     - `grep "h-screen" src/ -r` should return NOTHING (must use h-dvh)
     - `grep "tailwind.config" .` should return NOTHING (Tailwind v4 is CSS-first)
     - `grep "@theme inline" src/app/globals.css` should return a match
  7. **Auth flow check:**
     - `grep "isHydrated" src/lib/contexts/auth-context.tsx` should return a match
     - `grep "wildwood-role" src/lib/contexts/auth-context.tsx` should return a match
  8. **Banner check:**
     - `grep "PROTOTYPE" src/components/prototype-banner.tsx` should return a match
     - `grep "PrototypeBanner" src/app/layout.tsx` should return a match

  If ANY check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>All 8 verification categories pass without errors.</verify>
  <done>All automated checks pass: build, types, data integrity, design system conventions, auth flow, and banner presence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 foundation: project scaffold, WildWood design system (dual theme), auth with role toggle, POS and admin layout shells, realistic mock data, data access layer, prototype banner.</what-built>
  <how-to-verify>
  Start the dev server with `npm run dev` and open http://localhost:3000 in a browser.

  **1. Login Flow:**
  - You should see the login screen with "WILDWOOD" branding on a dark background
  - Two buttons: "Admin (Proprietaire)" and "Staff (Caisse POS)"
  - The PROTOTYPE banner should be visible at the very top

  **2. POS Layout (click "Staff"):**
  - Dark background (#1F1F1F), "WildWood POS" header
  - Page should fill the entire viewport (no scrollbar)
  - Two placeholder POS buttons ("Passes Gym" and "F&B") should be large (120x80px minimum)
  - Role toggle badge showing "Staff" in the header
  - Prototype banner still visible at top

  **3. Admin Layout (click role toggle badge to switch to Admin):**
  - Cream/sand background
  - Left sidebar with dark wood color showing 6 navigation links with icons
  - "Dashboard" link should be highlighted (active)
  - Main content area shows "Dashboard" placeholder text
  - Role toggle badge now showing "Admin"

  **4. Role Persistence:**
  - Refresh the page (F5) -- you should stay on the same layout (not redirected to login)
  - Close the tab and reopen http://localhost:3000 -- should redirect to the last used layout

  **5. Logout:**
  - Click the logout icon (LogOut) next to the role badge
  - Should return to the login screen

  **6. Design System:**
  - Colors feel warm and resort-like (not corporate/tech)
  - Oswald font visible on headings (condensed, bold)
  - Inter font on body text (clean, readable)
  - POS dark theme and Admin light theme are visually distinct
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
All Phase 1 success criteria from ROADMAP.md verified:
1. User can toggle between admin and staff roles, role persists across navigation
2. WildWood color palette applied globally, POS dark / Admin light
3. POS layout full viewport, no scroll, 120x80px minimum touch targets
4. Mock data with realistic pricing, accessed only through data-access.ts
5. PROTOTYPE banner visible on every screen
</verification>

<success_criteria>
Phase 1 is complete and verified. The foundation is ready for Phase 2 (POS Cash Register) development. All 5 roadmap success criteria pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-05-SUMMARY.md`
</output>
