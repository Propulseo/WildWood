---
phase: 04-core-admin-modules
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 10 requirements (CLI-01 through CLI-06, BUNG-01 through BUNG-04) are verifiable in the running application"
    - "npm run build succeeds with zero errors"
    - "All 5 phase success criteria pass visual and functional inspection"
  artifacts: []
  key_links: []
---

<objective>
Run automated verification checks and perform visual checkpoint for all Phase 4 deliverables.

Purpose: Confirm all 10 requirements are met before marking the phase complete.
Output: Verified phase with all success criteria passing.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of Phase 4 deliverables</name>
  <files></files>
  <action>
Run the following automated checks:

1. **Build check:** `npm run build` -- must succeed with zero errors and zero warnings
2. **File existence:** Verify all expected files exist:
   - `src/components/ui/table.tsx`
   - `src/components/ui/select.tsx`
   - `src/app/(admin)/clients/page.tsx`
   - `src/app/(admin)/clients/[id]/page.tsx`
   - `src/app/(admin)/bungalows/page.tsx`
3. **Import verification:** Grep for key patterns in each file:
   - clients/page.tsx: `useTransactions`, `getClients`, `Table`, `Select`, `router.push`
   - clients/[id]/page.tsx: `useParams`, `getClientById`, `toast`, `newsletter`, `Resident`
   - bungalows/page.tsx: `getBungalows`, `startOfMonth`, `locale.*fr`, `Dumbbell`, `occupancy` or `occupation`
4. **Route verification:** Start dev server if not running, then curl:
   - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/clients` -- expect 200
   - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/clients/cli-001` -- expect 200
   - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/bungalows` -- expect 200

Report results as a checklist. If any check fails, describe what's wrong and attempt to fix it.
  </action>
  <verify>All automated checks pass.</verify>
  <done>Build succeeds, all files exist with correct imports, all 3 routes return 200.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 4: Client database module (list + profile) and Bungalow calendar module.

**Client List (/clients):**
- Paginated table with 7 columns (nom, prenom, email, telephone, type de pass, derniere visite, nb visites)
- Search-as-you-type by name, email, or phone
- Dropdown filters for pass type and time period
- 10 items per page, 4 pages total
- Clickable rows navigate to client profile

**Client Profile (/clients/cli-001):**
- Client info header with avatar placeholder
- "Resident bungalow" badge for active residents
- Stats cards (montant total, visites, derniere visite)
- Purchase history table
- Visit dates as badges
- Newsletter toggle button with toast

**Bungalow Calendar (/bungalows):**
- Month view with 8 bungalow rows and day columns
- Colored reservation bars (lime=en-cours, orange=confirmee, muted=terminee, red=annulee)
- Dumbbell icon on active reservations (gym access)
- Month navigation with French month names
- Monthly + weekly occupancy rates
  </what-built>
  <how-to-verify>
1. Open http://localhost:3000/clients
   - Verify table shows 35 clients with all 7 columns populated
   - Type "Martin" in search -- results should filter live
   - Select a pass type from the dropdown -- results should filter
   - Click page 2, 3, 4 -- pagination works
   - Click any client row -- navigates to their profile

2. Open http://localhost:3000/clients/cli-001
   - Verify client name, email, phone displayed
   - Verify "Resident bungalow" badge is visible (cli-001 is a resident)
   - Verify stats cards show correct totals
   - Verify purchase history table shows transactions
   - Click "Inscrit/Ajouter a la newsletter" button -- toast appears

3. Open http://localhost:3000/clients/cli-015
   - Verify NO resident badge (cli-015 is a visiteur)

4. Open http://localhost:3000/bungalows
   - Verify 8 bungalow rows with reservation bars
   - Verify bars are color-coded by status
   - Verify dumbbell icon on lime (en-cours) bars
   - Click prev/next month arrows -- calendar updates
   - Verify month name is in French (e.g., "Mars 2026")
   - Verify occupancy percentages displayed below calendar

5. Check sidebar: "Clients" and "Bungalows" links should highlight when on their respective pages
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
All Phase 4 success criteria verified:
1. Client list shows paginated table with 7 columns, search, and filters
2. Filters work for pass type and time period
3. Client profile shows history, stats, and resident badge
4. Newsletter button works with toast
5. Bungalow calendar shows month view with reservations and occupancy
</verification>

<success_criteria>
All 10 requirements verified:
- CLI-01: Paginated client table with 7 columns
- CLI-02: Search by name/email/phone
- CLI-03: Filter by pass type or period
- CLI-04: Client profile with history, stats, visit dates
- CLI-05: Resident bungalow badge
- CLI-06: Newsletter toggle button
- BUNG-01: Monthly calendar with 8 bungalow rows
- BUNG-02: Reservation bars with client name, dates, nights, amount, status
- BUNG-03: Weekly/monthly occupancy rates
- BUNG-04: Gym access indicator (dumbbell) on active reservations
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-admin-modules/04-04-SUMMARY.md`
</output>
