---
phase: 03-admin-shell-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Admin sidebar collapses to icon-only mode when toggle button is clicked"
    - "Admin sidebar expands back to full width when toggle button is clicked again"
    - "Sidebar collapse state persists across page reloads via localStorage"
    - "Dashboard shows today's revenue total as a prominent stat card"
    - "Dashboard shows number of passes sold today as a prominent stat card"
    - "Dashboard shows number of new reservations today as a prominent stat card"
    - "Dashboard shows revenue breakdown by Gym, F&B, and Bungalows"
  artifacts:
    - path: "src/app/(admin)/layout.tsx"
      provides: "Collapsible sidebar with TransactionsProvider"
      contains: "isCollapsed"
    - path: "src/app/(admin)/dashboard/page.tsx"
      provides: "Dashboard with stat cards and revenue breakdown"
      contains: "useTransactions"
  key_links:
    - from: "src/app/(admin)/layout.tsx"
      to: "@/contexts/transactions-context"
      via: "TransactionsProvider wrapping children"
      pattern: "TransactionsProvider"
    - from: "src/app/(admin)/dashboard/page.tsx"
      to: "@/contexts/transactions-context"
      via: "useTransactions hook"
      pattern: "useTransactions"
---

<objective>
Verify Phase 3 implementation: collapsible sidebar and dashboard KPIs.

Purpose: Confirm all success criteria are met through automated checks and visual verification before marking Phase 3 complete.
Output: Verified Phase 3 with passing build and visual confirmation.
</objective>

<execution_context>
@C:\Users\etien\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\etien\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-shell-dashboard/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification checks</name>
  <files></files>
  <action>
Run the following automated checks to verify Phase 3 implementation:

1. `npm run build` -- must complete without errors
2. Verify admin layout has collapsible sidebar:
   - Grep `isCollapsed` in `src/app/(admin)/layout.tsx` -- must exist
   - Grep `TransactionsProvider` in `src/app/(admin)/layout.tsx` -- must exist
   - Grep `PanelLeftClose` in `src/app/(admin)/layout.tsx` -- must exist
   - Grep `transition-all` in `src/app/(admin)/layout.tsx` -- must exist
   - Grep `localStorage` in `src/app/(admin)/layout.tsx` -- must exist
   - Grep `w-16` in `src/app/(admin)/layout.tsx` -- must exist (collapsed width)
3. Verify dashboard page has stat cards:
   - Grep `use client` in `src/app/(admin)/dashboard/page.tsx` -- must exist
   - Grep `useTransactions` in `src/app/(admin)/dashboard/page.tsx` -- must exist
   - Grep `useMemo` in `src/app/(admin)/dashboard/page.tsx` -- must exist
   - Grep `isToday` in `src/app/(admin)/dashboard/page.tsx` -- must exist
   - Grep `toLocaleString` in `src/app/(admin)/dashboard/page.tsx` -- must exist
   - Grep `CardTitle` in `src/app/(admin)/dashboard/page.tsx` -- must exist
   - Grep `Revenus` in `src/app/(admin)/dashboard/page.tsx` -- must exist (French labels)
4. Verify no shadcn sidebar installed:
   - Confirm `src/components/ui/sidebar.tsx` does NOT exist
5. Verify Phase 3 success criteria from ROADMAP.md:
   - SC1: Admin layout has collapsible sidebar with 6 nav items (Dashboard, Clients, Bungalows, Comptabilite, Newsletter, Instagram)
   - SC2: Dashboard has stat cards for revenue, passes, reservations
   - SC3: Dashboard has revenue breakdown by 3 centers
  </action>
  <verify>All grep checks pass. Build succeeds. No shadcn sidebar.tsx file exists.</verify>
  <done>All automated verification checks pass for Phase 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 3: Admin Shell + Dashboard

1. Collapsible admin sidebar with icon-first navigation to 6 modules
2. Smooth CSS transition between expanded (256px) and collapsed (64px) states
3. Sidebar collapse persists across page reloads via localStorage
4. Dashboard stat cards: Revenus du jour, Passes vendus, Nouvelles reservations
5. Revenue breakdown by center: Gym, F&B, Bungalows
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and navigate to http://localhost:3000/dashboard
2. Verify the sidebar shows 6 navigation items with icons and French labels
3. Click the collapse toggle button at the bottom of the sidebar
4. Verify the sidebar smoothly collapses to icon-only mode
5. Hover over a collapsed icon -- verify tooltip shows the label
6. Click the toggle button again to expand -- verify smooth expansion
7. Reload the page -- verify sidebar remembers its collapsed/expanded state
8. Check the dashboard stat cards show:
   - Revenus du jour: 21,590 THB
   - Passes vendus: 6
   - Nouvelles reservations: 0
9. Check the revenue breakdown shows:
   - Gym: 19,600 THB
   - F&B: 1,990 THB
   - Bungalows: 0 THB
10. Verify the prototype banner is still visible
11. Verify the RoleToggle in the header still works
12. Resize the browser to tablet width (~1024px) -- verify layout doesn't break
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Build passes without errors
2. All grep checks confirm correct patterns in source files
3. Human verifies visual appearance and interactive behavior
4. Dashboard values match expected mock data
</verification>

<success_criteria>
- All automated checks pass
- Human confirms sidebar collapses/expands smoothly
- Human confirms dashboard shows correct KPIs
- Human confirms layout works on tablet viewport
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-shell-dashboard/03-02-SUMMARY.md`
</output>
